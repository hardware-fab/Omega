# DSE

This directory contains several python and bash scripts for the automation of the design space exploration.

- `configuration` contains a set of scripts for executing operations on configuration files, like converting from different configuration formats or generating a random SoC configuration
- `execution` contains a set of scripts to automate the testing of a configuration on an FPGA board
- `generic` contains generic python modules that are used by other scripts
- `implementation` contains a bash script that starts the implementation and bitstream generation of a SoC configuration
- `models` contains various example scripts for the generation of a design space 
